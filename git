Initialized repository   ->  git init
to add the files ->  git add
to cimmit the files  ->   git commit -m "common information"
to push these files into your repositories that are existed in github, we need a github accout. and create a repository in that
git add origin master <URL OF THAT REPOSITORY>
git push origin master
 
to clone the files from the github   ->   git pull origin master
to add the branch   ->   git branch <name>
to switch the branch   ->    git checkout <branchname>
to make unchanges   ->   git stash
for saving the changes that are deleted  ->   git stash pop
for undoing the changes ->  git revert commitid
to see the diff b/w commits  ->  git diff  idx idy

